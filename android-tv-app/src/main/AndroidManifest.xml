<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The bundled mpv payload declares minSdk 26; AndroidMpvRuntimeAvailability
         refuses mpv construction below API 26 so the app itself can run on 25. -->
    <uses-sdk tools:overrideLibrary="dev.jdtech.mpv" />

    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.android.providers.tv.permission.WRITE_EPG_DATA" />

    <!-- Cleartext HTTP is a core Jellyfin use case: most home servers run
         plain http on the LAN (e.g. http://192.168.x.x:8096). -->
    <application
        android:name=".TvApplication"
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="jellyscope" android:host="play" />
            </intent-filter>
        </activity>
        <provider
            android:name=".watchnext.WatchNextPosterProvider"
            android:authorities="${applicationId}.watchnext.poster"
            android:exported="true" />
    </application>

</manifest>
